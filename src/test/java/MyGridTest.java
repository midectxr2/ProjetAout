import com.emile.projetaout.logiquetest.Boat;
import com.emile.projetaout.logiquetest.Direction;
import com.emile.projetaout.logiquetest.Game;
import com.emile.projetaout.logiquetest.Grid;
import org.junit.Test;

import static org.junit.Assert.assertEquals;

/*
public class MyGridTest {
    @Test
    public void loadGridTest() {
        Game game = new Game();
        try {
            game.loadGame("src\\main\\java\\com\\emile\\projetaout\\logiquetest\\grid.txt");
            Grid grid = new Grid(10, 10);
            Boat boat = new Boat(2);
            Boat boat3 = new Boat(3);
            grid.placeBoat(boat, 2, 0, Direction.VERTICAL);
            grid.placeBoat(boat3, 0, 2, Direction.HORIZONTAL);
            assertEquals(game.getPlayersList().get(0).getGrid(), grid);

        }
        catch (Exception exception){

        }
    }
}
*/
